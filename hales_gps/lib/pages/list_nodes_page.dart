import 'package:flutter/material.dart';
import 'package:hales_gps/theme.dart';

class ListNodesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Lista de Nós",
        style: TextStyle(fontSize: 24, color: Colors.white),
      ),
    );
  }
}
